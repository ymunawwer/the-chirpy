# API Documentation (`doc_apis`)

This folder contains human-readable documentation for the main HTTP APIs exposed by this service.

- `auth.md` – Authentication and token APIs (`/auth/*`).
- `users.md` – User management APIs (`/users/*`).
- `events.md` – Event management APIs (`/events/*`).
- `contacts.md` – Contact management APIs (`/contacts/*`).
- `callLogs.md` – Call log query APIs (`/call-logs/*`).

These docs are derived from the current Express route definitions and Swagger annotations.
